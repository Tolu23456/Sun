component Button {
  style {
    .btn { padding: 0.5rem 1rem; border: none; border-radius: 4px; background: #f5a623; color: #000; cursor: pointer; }
    .btn:hover { opacity: 0.8; }
  }

  render {
    <button .btn>
      <slot />
    </button>
  }
}
